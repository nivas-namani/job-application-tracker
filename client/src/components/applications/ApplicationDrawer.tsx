import { ChevronDown, Trash2, Upload, X } from 'lucide-react';
import { useRef, useState } from 'react';
import { displayDate, displayMoney } from '../../lib/application-utils';
import { APPLICATION_STATUSES, statusMeta, type Application, type ApplicationStatus, type Resume } from '../../types';
import { Detail } from './Detail';
import { DrawerSection } from './DrawerSection';
import { ResumeRow } from './ResumeRow';

type ApplicationDrawerProps = { application: Application; resumes: Resume[]; onClose: () => void; onStatusChange: (id: string, status: ApplicationStatus) => Promise<void>; onEdit: () => void; onDelete: () => void; onUpload: (file: File) => Promise<void> };

export function ApplicationDrawer({ application, resumes, onClose, onStatusChange, onEdit, onDelete, onUpload }: ApplicationDrawerProps) {
  const fileInput = useRef<HTMLInputElement>(null);
  const [uploading, setUploading] = useState(false);
  const salary = [displayMoney(application.salaryMin, application.currency), displayMoney(application.salaryMax, application.currency)].filter(Boolean).join(' – ');

  const uploadResume = async (file?: File) => {
    if (!file) return;
    setUploading(true);
    try { await onUpload(file); } finally { setUploading(false); if (fileInput.current) fileInput.current.value = ''; }
  };

  return <div className="drawer-backdrop" onMouseDown={onClose}><aside className="scrollbar-thin absolute right-0 top-0 flex h-full w-full max-w-[520px] flex-col overflow-y-auto bg-white shadow-soft" onMouseDown={(event) => event.stopPropagation()} aria-label={`${application.company} details`}><header className="border-b px-6 py-5"><div className="flex items-start justify-between gap-4"><div><h2 className="text-2xl font-extrabold tracking-tight">{application.company}</h2><p className="mt-1 text-sm text-muted">{application.role}{application.location ? ` · ${application.location}` : ''}</p></div><button aria-label="Close details" onClick={onClose} className="rounded-lg p-1 text-muted hover:bg-slate-100"><X className="h-5 w-5" /></button></div><div className="mt-4 flex items-center gap-3"><span className={`inline-flex items-center gap-1.5 rounded-full px-3 py-1.5 text-xs font-bold ${statusMeta[application.status].pill}`}><span className={`h-1.5 w-1.5 rounded-full ${statusMeta[application.status].dot}`} />{statusMeta[application.status].label}</span><label className="inline-flex items-center gap-1 rounded-lg border px-2.5 py-1.5 text-xs font-bold">Move to <select aria-label="Move application status" value={application.status} onChange={(event) => void onStatusChange(application.id, event.target.value as ApplicationStatus)} className="max-w-28 bg-transparent outline-none">{APPLICATION_STATUSES.map((status) => <option key={status} value={status}>{statusMeta[status].label}</option>)}</select><ChevronDown className="h-3.5 w-3.5" /></label></div></header><div className="flex-1 space-y-7 px-6 py-6"><DrawerSection title="Details"><div className="grid grid-cols-2 gap-x-5 gap-y-5 text-sm"><Detail label="Applied" value={displayDate(application.appliedAt)} /><Detail label="Source" value={application.source ?? 'Not set'} /><Detail label="Salary range" value={salary || 'Not set'} /><Detail label="Follow up" value={displayDate(application.followUpAt)} /><Detail label="Location" value={application.location ?? 'Not set'} />{application.jobUrl && <Detail label="Job posting" value={<a className="break-all font-semibold text-violet hover:underline" href={application.jobUrl} target="_blank" rel="noreferrer">Open posting ↗</a>} />}</div></DrawerSection><DrawerSection title="Notes"><p className="min-h-24 whitespace-pre-wrap rounded-xl border bg-slate-50 p-4 text-sm leading-6 text-slate-700">{application.description || 'No notes yet.'}</p></DrawerSection><DrawerSection title="Resume used">{application.resume ? <ResumeRow resume={application.resume} /> : <div><p className="mb-3 text-sm text-muted">No resume attached to this application.</p><button onClick={() => fileInput.current?.click()} disabled={uploading} className="button-secondary"><Upload className="h-4 w-4" />{uploading ? 'Uploading…' : 'Upload and attach'}</button></div>}<input ref={fileInput} className="hidden" type="file" accept=".pdf,.doc,.docx,application/pdf,application/msword,application/vnd.openxmlformats-officedocument.wordprocessingml.document" onChange={(event) => void uploadResume(event.target.files?.[0])} />{!application.resume && resumes.length > 0 && <p className="mt-3 text-xs text-muted">You can also choose an existing resume when editing this application.</p>}</DrawerSection><DrawerSection title="Status history"><ol className="space-y-4 border-l border-slate-200 pl-4">{application.statusHistory.map((item) => <li key={item.id} className="relative text-sm"><span className={`absolute -left-[21px] top-1 h-3 w-3 rounded-full border-2 border-white ${statusMeta[item.toStatus].dot}`} /><p className="font-bold">{item.fromStatus ? `${statusMeta[item.fromStatus].label} → ` : ''}{statusMeta[item.toStatus].label}</p><p className="mt-0.5 text-xs text-muted">{displayDate(item.changedAt)}</p></li>)}</ol></DrawerSection></div><footer className="sticky bottom-0 flex items-center gap-3 border-t bg-white px-6 py-4"><button onClick={onEdit} className="button-primary">Edit</button><button onClick={onDelete} className="button-danger ml-auto"><Trash2 className="h-4 w-4" />Delete</button></footer></aside></div>;
}
